// Copy this file to `secrets.dart` and fill in the values below.
// `secrets.dart` is gitignored so your real keys stay out of source control.
//
// The YouTube Music key below is the well-known PUBLIC key used by every
// unofficial YT Music client (it lives in YT Music's own HTML). It is not
// a private credential, but GitHub's secret scanner flags any AIza* literal,
// so we keep it out of the tracked source.
const String ytMusicApiKey = 'YOUR_YOUTUBE_MUSIC_API_KEY';
